# Groq API Key for natural language timetable instructions and conflict explanations
GROQ_API_KEY=""

# Optional Database URL (defaults to sqlite:///./timetable.db)
DATABASE_URL="sqlite:///./timetable.db"

# Server Host and Port
HOST="0.0.0.0"
PORT=8000
